# === veed.io (render) ===
# VEED_API_KEY: veed.io → Profile menu → API Keys → Create key
VEED_API_KEY=
# VEED_TEMPLATE_ID: open your chosen vertical template in veed.io, copy template_id from the URL or API response
VEED_TEMPLATE_ID=
# VEED_VOICE_ID: from veed.io voice library (check voice id in TTS settings)
VEED_VOICE_ID=
# FORGEFINDS_LOGO_URL: public URL to your logo (e.g., https://yourcdn/forgefinds-logo.png)
FORGEFINDS_LOGO_URL=

# === OpenAI (script) ===
# OPENAI_API_KEY: platform.openai.com → API Keys → Create new secret key
OPENAI_API_KEY=
# OPENAI_MODEL: defaults to gpt-4o-mini; override if you prefer
OPENAI_MODEL=gpt-4o-mini

# === YouTube (upload) ===
# Steps: Google Cloud Console → create project → enable YouTube Data API v3 → OAuth consent screen (External) → OAuth client (Desktop)
# Then use that client to generate a refresh token (e.g., with a one-time OAuth flow) and paste here.
YT_CLIENT_ID=
YT_CLIENT_SECRET=
YT_REFRESH_TOKEN=
# YT_CHANNEL_ID: YouTube Studio → Customization → Basic info → Channel URL, copy the UC… id
YT_CHANNEL_ID=

# Optional: analytics/telemetry off by default
DISABLE_TELEMETRY=true